<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>更新學生資料</title>
  </head>
  <body>
    <form action="/students/<%= foundStudent.id %>?_method=PUT" method="POST">
      <label for="name">姓名：</label>
      <input
        type="text"
        name="name"
        id="name"
        value="<%= foundStudent.name %>"
      />
      <label for="age">年齡：</label>
      <input
        type="number"
        name="age"
        id="age"
        value="<%= foundStudent.age %>"
      />
      <label for="merit">merit 獎學金：</label>
      <input
        type="number"
        name="merit"
        id="merit"
        value="<%= foundStudent.scholarship.merit %>"
      />
      <label for="other">other 獎學金：</label>
      <input
        type="number"
        name="other"
        id="other"
        value="<%= foundStudent.scholarship.other %>"
      />
      <button>更新學生資料</button>
    </form>
  </body>
</html>
